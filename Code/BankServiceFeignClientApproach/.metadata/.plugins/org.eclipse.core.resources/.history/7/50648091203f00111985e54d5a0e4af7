package com.bank.account.feign;
import com.bank.account.config.FeignConfig;

import java.util.ArrayList;
import java.util.List;

import org.springframework.cloud.openfeign.FeignClient;
import io.github.resilience4j.circuitbreaker.annotation.CircuitBreaker;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;

import com.bank.account.dto.Card;
@FeignClient(name = "card-service" ,  configuration = FeignConfig.class
		)
//@FeignClient(name = "card-service", url = "http://localhost:8081")
public interface CardClient {

	 @GetMapping("/cards/account/{accountId}")
	    List<Card> getCards(@PathVariable int accountId);
    }
